name: Auto Promote to Higher Environment

on:
  pull_request:
    types: [closed]
    branches:
      - master
      - pre-staging
      - staging

jobs:
  promote:
      if: >
        github.event.pull_request.merged == true &&
        contains(github.event.pull_request.labels.*.name, 'auto-promote-changes-till-prod')
      runs-on: ubuntu-latest
      
      steps:
      - name: Notify Slack
        uses: slackapi/slack-github-action@v1.25.0
        with:
          payload: |
            {
              "channel": "C09DX2EU09E",
              "text": ":rocket: An auto promotion PR was created from *${{ steps.set-branches.outputs.source }}* → *${{ steps.set-branches.outputs.target }}* and auto-merge was enabled."
            }
        env:
          SLACK_BOT_TOKEN: xoxb-14134705664-9445457896674-CgnhnaaAX8mz0FmKvmJEDRor
